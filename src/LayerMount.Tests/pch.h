#pragma once

// windows.h must come first so transitive engine includes see the
// expected Win32 declarations.
#include <windows.h>

#include <CppUnitTest.h>

#include <string>
#include <vector>
#include <optional>
#include <memory>
#include <filesystem>
#include <thread>
#include <atomic>
#include <chrono>
